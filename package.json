{
  "name": "http-signed-fetch",
  "version": "1.0.0",
  "description": "Easy to use module for signing http requests for ActivityPub using the `fetch` API",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard --fix"
  },
  "keywords": [
    "activitypub",
    "fetch",
    "digest",
    "signature",
    "http"
  ],
  "author": "RangerMauve",
  "license": "MIT",
  "dependencies": {
    "@digitalbazaar/http-digest-header": "^2.0.0",
    "activitypub-http-signatures": "^2.0.1"
  },
  "devDependencies": {
    "standard": "^17.1.0",
    "tape": "^5.7.2"
  }
}
